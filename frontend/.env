# API Configuration
# For development, point to local backend (without /api suffix, it's added automatically)
VITE_API_URL=http://localhost:8080

# For production, use relative path or production URL (without /api suffix)
# VITE_API_URL=
# VITE_API_URL=https://api.veidly.com
