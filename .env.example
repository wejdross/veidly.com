# Server Configuration
PORT=8080

# JWT_SECRET must be at least 43 characters (NIST recommendation for HMAC-SHA256)
# Generate with: openssl rand -base64 43
JWT_SECRET=your-super-secret-jwt-key-at-least-43-characters-generate-with-openssl

# CSRF_SECRET must be at least 32 characters
# Generate with: openssl rand -base64 32
CSRF_SECRET=your-csrf-secret-at-least-32-characters-generate-with-openssl

ADMIN_PASSWORD=change-this-secure-password-for-admin

# CORS Configuration (comma-separated origins, no wildcards allowed)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# TLS/HTTPS Configuration (optional, for production)
USE_TLS=false
TLS_CERT=/path/to/cert.pem
TLS_KEY=/path/to/key.pem

# Environment
ENVIRONMENT=development

# Mailgun Email Configuration
MAILGUN_DOMAIN=your-domain.mailgun.org
MAILGUN_API_KEY=your-mailgun-api-key
MAILGUN_FROM_EMAIL=noreply@veidly.com
BASE_URL=http://localhost:5173
